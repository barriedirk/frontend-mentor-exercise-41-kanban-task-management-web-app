npm create next-app frontend --typescript

pnpm add zustand

ipnpm add @tanstack/react-query

pnpm add -D @types/react

pnpm add @tanstack/react-query-devtools

pnpm add react-hook-form zod @hookform/resolvers

pnpm dlx shadcn@latest init

# read https://ui.shadcn.com/docs/installation/next



pnpm dev	Levanta la app en modo desarrollo
pnpm build	Compila la app para producción
pnpm start	Corre la versión compilada (después de build)
pnpm lint	Ejecuta ESLint
pnpm format	Formatea el código (si tienes prettier configurado)



backend
-----------


npx create-strapi-app@latest
# pnpm create strapi-app backend --quickstart #sqlite

pnpm rebuild better-sqlite3
pnpm add pg
pnpm run develop

http://localhost:1337/adminp

